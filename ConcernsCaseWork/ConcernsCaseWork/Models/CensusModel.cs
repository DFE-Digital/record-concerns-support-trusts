namespace ConcernsCaseWork.Models
{
	/// <summary>
	/// Frontend model classes used only for UI rendering
	/// </summary>
	public sealed class CensusModel
	{
		public string NumberOfPupils { get; }

		public CensusModel(string numberOfPupils) => 
			(NumberOfPupils) = 
			(numberOfPupils);
	}
}