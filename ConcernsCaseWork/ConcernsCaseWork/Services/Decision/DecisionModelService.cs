using ConcernsCaseWork.Mappers;
using ConcernsCaseWork.Models.CaseActions;
using System;
using System.Threading.Tasks;

namespace ConcernsCaseWork.Services.Decision
{
	public class DecisionModelService 
	{
		/*

		private readonly IDecisionCachedService _decisionCachedService;

		public DecisionModelService(IDecisionCachedService decisionCachedService)
		{
			_decisionCachedService = decisionCachedService;
		}

		public async Task<DecisionModel> CreateDecision(DecisionModel decisionModel)
		{
			var created = await _decisionCachedService.CreateDecision(DecisionMappers.ToDBModel(decisionModel));

			return DecisionMappers.ToServiceModel(created);
		}


		*/
	}
}

